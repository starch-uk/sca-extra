<?xml version="1.0" ?>
<ruleset
	name="Apex Naming Rules"
	xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd"
>
	<description>Custom rules for Apex naming conventions</description>

	<rule
		name="NoSingleLetterVariableNames"
		language="apex"
		message="Single-letter variable names are not allowed except for loop counters (i, c) or exception variables (e). Use descriptive names instead"
		class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
	>
		<description>
			Single-letter variable names are not allowed except for loop counters (i, c) or
			exception
			variables (e). Use descriptive names instead to improve code readability and make the code
			self-documenting. Single-letter names provide no context about the variable's purpose.
		</description>
		<priority>2</priority>
		<properties>
			<property name="xpath">
				<value>
                    <![CDATA[
                    //VariableDeclaration[
                        string-length(@Image) = 1
                        and @Image != 'i'
                        and @Image != 'c'
                        and not(ancestor::CatchBlockStatement)
                    ]
                    ]]>
                </value>
			</property>
		</properties>
	</rule>
</ruleset>

<?xml version="1.0" ?>
<ruleset
	name="Apex Modifiers Rules"
	xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd"
>
	<description>Custom rules for Apex modifiers and variable declarations</description>
	<rule
		name="RegexPatternsMustBeStaticFinal"
		language="apex"
		message="Regular expression patterns must be declared as static final constants instead of inline for better performance and maintainability"
		class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
	>
		<description>
			Regular expression patterns should be declared as static final constants, not
			inline. This
			improves performance by avoiding repeated pattern compilation, makes patterns reusable,
			and improves maintainability by centralizing pattern definitions.
		</description>
		<priority>2</priority>
		<properties>
			<property name="xpath">
				<value>
					<![CDATA[
					//MethodCallExpression[
                        (
                            (@MethodName = 'compile' and @FullMethodName = 'Pattern.compile')
                            or (@MethodName = 'matches')
                        )
                        and .//LiteralExpression[@String = true()]
                        and not(ancestor::FieldDeclarationStatements[ModifierNode[@Static = true() and @Final = true()]])
                        and not(ancestor::Field[ModifierNode[@Static = true() and @Final = true()]])
                    ]
					]]>
				</value>
			</property>
		</properties>
		<example>
			<![CDATA[
			// Violation: Inline regex pattern
			public Boolean isValid(String input) {
				return Pattern.matches('^[A-Z]+$', input);
			}
			
			// Valid: Static final regex pattern
			private static final String PATTERN = '^[A-Z]+$';
			public Boolean isValid(String input) {
				return Pattern.matches(PATTERN, input);
			}
			]]>
		</example>
	</rule>
</ruleset>
